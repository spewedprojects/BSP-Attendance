{
  "name": "bsp-attendance",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "cap:sync": "cap sync android",
    "cap:copy": "cap copy android",
    "android:build": "npm run build && npx cap sync android && .\\gradlew assembleDebug",
    "android:run": "npm run build && npx cap sync android && .\\gradlew installDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.1",
    "@capacitor/core": "^8.5.1",
    "@capacitor/filesystem": "^8.1.3",
    "@capacitor/preferences": "^8.0.1",
    "@capacitor/share": "^8.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.1",
    "typescript": "^5.8.2",
    "vite": "^6.2.0"
  }
}
