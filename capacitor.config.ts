import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gratus.bspattendance',
  appName: 'BSP Attendance',
  webDir: 'dist',
  android: {
    path: '.',
  },
};

export default config;
